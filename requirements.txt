# Create and activate your virtual environment
# Then install dependencies with pip install -r requirements.txt

flask
torch
transformers
requests
beautifulsoup4
aiohttp
asyncio
# gunicorn    Optional: For production server if deploying